% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/furl_stream.R
\name{furl_stream}
\alias{furl_stream}
\title{Concurrently stream Server-Sent Events from many endpoints}
\usage{
furl_stream(
  urls,
  on_event = NULL,
  method = "POST",
  body = NULL,
  content_type = NULL,
  headers = NULL,
  useragent = NULL,
  timeout = 0,
  accept_encoding = "gzip",
  parse = c("auto", "raw"),
  collect = TRUE,
  total_con = 100L,
  host_con = 6L,
  multiplex = TRUE
)
}
\arguments{
\item{urls}{Character vector of streaming endpoints.}

\item{on_event}{Optional function called for every event as it arrives, as
\code{on_event(event, index)}. \code{event} is a list with elements \code{event} (the SSE
event type, \code{"message"} if unset), \code{data} (parsed per \code{parse}), \code{id}, and
\code{url}; \code{index} is the position in \code{urls}. May be \code{NULL} to only collect.}

\item{method, body, content_type, headers, useragent, timeout, accept_encoding}{As in \code{\link[=furl_download]{furl_download()}}. \code{method} defaults to \code{"POST"}. \code{body} follows the
same broadcast/per-URL rules and JSON serialisation.}

\item{parse}{How to deliver each event's \code{data}: \code{"auto"} (default) parses it
as JSON with the fastest installed backend, falling back to the raw string
if it is not valid JSON (and never parsing the \verb{[DONE]} sentinel); \code{"raw"}
always delivers the raw string.}

\item{collect}{If \code{TRUE} (default), each successful slot of the result holds
the list of that stream's events; if \code{FALSE}, it holds the integer event
count (use this to avoid retaining large streams in memory).}

\item{total_con, host_con, multiplex}{Concurrency tuning passed to
\code{\link[curl:multi]{curl::new_pool()}}.}
}
\value{
A list aligned 1:1 with \code{urls}: collected events (or a count) for
successes, \code{furl_error} for failures.
}
\description{
\code{furl_stream()} opens many streaming HTTP connections at once and delivers
\href{https://developer.mozilla.org/docs/Web/API/Server-sent_events}{Server-Sent Events}
(\code{text/event-stream}) to an \code{on_event} callback as they arrive, while keeping
the same \strong{order-preserving, drop-nothing} contract as \code{\link[=furly]{furly()}}: the
returned list is aligned 1:1 with \code{urls}, with a \code{furl_error} in the slot of
any URL that failed.
}
\details{
This is the streaming counterpart to \code{\link[=furly]{furly()}}, built for token-streaming
LLM APIs and other event feeds. Such endpoints are almost always \code{POST} with
a JSON body, so \code{method} defaults to \code{"POST"}; pass \code{method = "GET"} for a
classic \code{EventSource}-style feed.

Because a partially consumed stream cannot be safely replayed, streams are
\strong{not retried} (unlike \code{\link[=furly]{furly()}}); a connection-level failure yields a
\code{furl_error} in that slot.
}
\examples{
\dontrun{
# Stream tokens from an OpenAI-style chat endpoint, printing deltas live.
furl_stream(
  "https://api.example.com/v1/chat/completions",
  on_event = function(ev, i) if (!identical(ev$data, "[DONE]"))
    cat(ev$data$choices[[1]]$delta$content),
  headers = c(Authorization = "Bearer <token>"),
  body = list(model = "x", stream = TRUE,
              messages = list(list(role = "user", content = "hi")))
)
}
}
\seealso{
\code{\link[=furly]{furly()}} for buffered (non-streaming) requests.
}
