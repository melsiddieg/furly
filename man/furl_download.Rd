% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/furl.R
\name{furl_download}
\alias{furl_download}
\title{Download many URLs concurrently}
\usage{
furl_download(
  urls,
  headers = NULL,
  timeout = 0,
  useragent = NULL,
  max_tries = 3L,
  backoff = 1,
  total_con = 100L,
  host_con = 6L,
  multiplex = TRUE,
  progress = FALSE,
  destfiles = NULL,
  engine = c("curl", "crul")
)
}
\arguments{
\item{urls}{Character vector of URLs to download.}

\item{headers}{Named character vector of request headers, e.g.
\code{c(Authorization = "Bearer <token>")}. \code{NULL} for none.}

\item{timeout}{Per-request timeout in seconds (\code{0} = no timeout).}

\item{useragent}{Optional \code{User-Agent} string.}

\item{max_tries}{Maximum attempts per URL (>= 1). Only transient failures are
retried.}

\item{backoff}{Base backoff in seconds; attempt \emph{k} waits
\code{backoff * 2^(k - 1)}.}

\item{total_con, host_con, multiplex}{Concurrency tuning passed to
\code{\link[curl:multi]{curl::new_pool()}}: total simultaneous connections, per-host limit, and
HTTP/2 multiplexing. Applies to \code{engine = "curl"} only.}

\item{progress}{Show a text progress bar.}

\item{destfiles}{Optional character vector of file paths, the same length as
\code{urls}. When supplied, each downloaded body is written to the corresponding
path (useful for saving binary payloads). The returned list then contains
the destination paths for successful downloads instead of response objects.}

\item{engine}{Concurrency backend: \code{"curl"} (default) drives \code{curl}'s
asynchronous multi interface directly; \code{"crul"} uses the optional
\code{\link[crul:crul-package]{crul}} package's \code{AsyncVaried} interface over the
same libcurl core. Both preserve order, report per-URL failures, and retry
transient errors identically.}
}
\value{
A list aligned 1:1 with \code{urls}. Successful elements are response
objects (\code{curl} responses for \code{engine = "curl"}, \code{crul::HttpResponse}
objects for \code{engine = "crul"}; both expose \verb{$status_code} and \verb{$content}),
or destination paths when \code{destfiles} is used. Failed elements are
\code{furl_error} objects. Retrieve just the failures with \code{\link[=furl_errors]{furl_errors()}}.
}
\description{
\code{furl_download()} fetches a vector of URLs in parallel using \code{curl}'s
asynchronous multi interface and HTTP/2 multiplexing. Unlike a naive async
loop it \strong{preserves input order}, \strong{never silently drops failures} (each
failed URL yields a \code{furl_error} in its slot), and \strong{retries transient
errors} (network failures, HTTP 429, HTTP 5xx) with exponential backoff.
}
\examples{
\dontrun{
urls <- sprintf("https://httpbin.org/get?i=\%d", 1:5)
res <- furl_download(urls)
furl_errors(res)                 # any failures?
}
}
\seealso{
\code{\link[=furly]{furly()}} for the JSON convenience wrapper, \code{\link[=furl_errors]{furl_errors()}}.
}
